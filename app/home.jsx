import { Text, View } from "react-native";

const home = () => {
    return (
        <View style={{ height: "100%" }}>
            <Text>Home Page</Text>
        </View>
    );
};

export default home;
